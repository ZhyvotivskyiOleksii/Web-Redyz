// This file is intentionally left blank. 
// The middleware handles redirection to the default locale.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
